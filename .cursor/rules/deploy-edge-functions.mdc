---
description: Al dar instrucciones de actualizar/desplegar Edge Functions, dar siempre estos comandos listos para copiar y pegar
globs: supabase/functions/**/*.ts,supabase/scripts/**/*
alwaysApply: false
---

# Desplegar Edge Functions en el servidor

Cuando el usuario pida instrucciones para actualizar las Edge Functions en el servidor, o comandos para desplegar funciones, responde con los comandos listos para copiar y pegar. Usa la documentación del proyecto como referencia.

**Documento de referencia:** [docs/05-integraciones-automatizacion.md](../../docs/05-integraciones-automatizacion.md) (§ Despliegue de Edge Functions self-hosted).

**Comando estándar (servidor SSH, una línea):**

```bash
cd /home/alex/Timeboxing && git pull && rsync -a ./supabase/functions/ /home/alex/supabase-pi/supabase/docker/volumes/functions/ && docker restart supabase-edge-functions
```

Si la ruta del repo en el servidor es otra, sustituye `/home/alex/Timeboxing`. El volumen de destino es `/home/alex/supabase-pi/supabase/docker/volumes/functions/`.

**Para una sola función:**

```bash
rsync -a /home/alex/Timeboxing/supabase/functions/<nombre-funcion> \
      /home/alex/supabase-pi/supabase/docker/volumes/functions/ \
   && docker restart supabase-edge-functions
```

**Ver logs tras el deploy:** `docker logs supabase-edge-functions --tail 50`

**No recomendar** `deploy-edge-functions-supabase-pi.sh` como flujo habitual: el comando con `rsync -a` es el que usa el equipo. El script hace esencialmente lo mismo (copia + reinicio) pero vía `docker compose restart functions` y añade comprobaciones extra (permisos con `sudo rsync`, alias DNS `functions`); solo mencionarlo si falla el alias de red Kong → Edge Runtime.
