# Copia las VITE_* del .env en la raíz del repo (Timeboxing), o define aquí en desarrollo.
# vite.config.ts usa envDir apuntando a ../../../
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Vacío en producción (nginx proxy /api/ → review-api:3001)
VITE_REVIEW_API_URL=http://localhost:3001
